{% extends 'base.html.twig' %}

{% block title %}Edit Tag{% endblock %}

{% block body %}
    <h1>Edit Tag</h1>

    {{ include('admin/tag/_form.html.twig', {'button_label': 'Update'}) }}
<div class="flex gap-3 mt-4 justify-center">

    <a href="{{ path('app_tag_index') }}" class="  p-3  bg-gray-400 text-white rounded-md hover:bg-gray-500 focus:outline-none focus:ring-2 focus:ring-gray-300">back to list</a>

    {{ include('admin/tag/_delete_form.html.twig') }}
    </div>
{% endblock %}
