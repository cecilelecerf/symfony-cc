{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Edit User</h1>

    {{ include('admin/user/_form.html.twig', {'button_label': 'Update'}) }}

<div class="flex gap-3 mt-4 justify-center">
 
            <a href="{{ path('app_user_index') }}" class="  p-3  bg-gray-400 text-white rounded-md hover:bg-gray-500 focus:outline-none focus:ring-2 focus:ring-gray-300">
                Back to list
            </a> 
  

    {{ include('admin/user/_delete_form.html.twig') }}
    </div>
{% endblock %}
