{% extends 'base.html.twig' %}

{% block title %}Edit Publication{% endblock %}

{% block body %}
    <h1 class="text-2xl font-bold text-center mb-6">Edit Publication</h1>

    {{ include('admin/publication/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="flex gap-3 mt-6 justify-center">
        <a href="{{ path('app_publication_index') }}" 
           class="p-3 bg-gray-400 text-white rounded-md hover:bg-gray-500 focus:outline-none focus:ring-2 focus:ring-gray-300">
            Back to List
        </a> 

        {{ include('admin/publication/_delete_form.html.twig') }}
    </div>
{% endblock %}
