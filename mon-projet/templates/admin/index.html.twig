{% extends 'base.html.twig' %}

{% block title %}User Index{% endblock %}

{% block body %}
<div  class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6"> 
                        <a href={{ path('app_user_index' ) }} class="bg-white rounded-lg shadow-md overflow-hidden transition transform hover:scale-105 hover:shadow-xl cursor-pointer">
                            <h2 class="text-2xl font-semibold text-gray-900 text-center py-3">User</h2>
                        </a> 

                                                <a href={{ path('app_tag_index' ) }} class="bg-white rounded-lg shadow-md overflow-hidden transition transform hover:scale-105 hover:shadow-xl cursor-pointer">
                            <h2 class="text-2xl font-semibold text-gray-900 text-center py-3">Tag</h2>
                        </a> 
                                                                 <a href={{ path('app_publication_index' ) }} class="bg-white rounded-lg shadow-md overflow-hidden transition transform hover:scale-105 hover:shadow-xl cursor-pointer">
                            <h2 class="text-2xl font-semibold text-gray-900 text-center py-3">Publication</h2>
                        </a> 
</div>
{% endblock %}
